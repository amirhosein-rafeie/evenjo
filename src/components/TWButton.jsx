import { styled } from "@mui/material/styles";
import Button from "@mui/material/Button";

const TWButton = styled(Button)(() => ({
  backgroundColor: "#C14FE6",
  color: "white",
  fontSize: "16px",
  borderRadius: "8px",
  textTransform: "lowercase",
  width: "80px"
}));

export default TWButton;